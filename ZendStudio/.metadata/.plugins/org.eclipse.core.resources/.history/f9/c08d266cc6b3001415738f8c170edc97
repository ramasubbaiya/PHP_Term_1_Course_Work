<?php
function loadComments()
{
    $array=array();
    
    $file=glob("*.comment");
   foreach($file as $k=>$v)
    $array[]=file_get_contents($v);
    //foreach($load as $x=>$y)
        
    $load[2]=explode("|",$array[$k]);
    print_r($load);
//echo $load;        
     
}loadComments();