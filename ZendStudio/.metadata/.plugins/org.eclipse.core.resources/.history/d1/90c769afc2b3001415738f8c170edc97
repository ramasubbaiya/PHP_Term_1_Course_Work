<?php
function loadComments()
{
    $array=array();
    $file=glob("*.comment");
   foreach($file as $k=>$v)
    $array=file_get_contents($v);
    
    //explode('|',$array);
    
echo $array;        
     
}loadComments();